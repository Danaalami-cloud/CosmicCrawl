// DEPRECATED: superseded by ./themes.ts, which generalizes "decade" into a
// broader theme system (decades + non-decade vibes + custom user text).
// Kept as a thin re-export so nothing breaks if something still imports it.
export { DECADE_THEMES as DECADES } from "./themes";
